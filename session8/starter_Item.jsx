/*
  Starter file for Session 32 Lab — Part 1 (Guided React Practice)
  Web Application Programming (G247) · CUNEF Escuela Politécnica Superior

  Week 11 · Session 32 · Practice (AF2) · Pair work

  Do NOT rename the component (Item) and do NOT change the prop name
  (item). The checklist assumes them. You only fill the body where the
  TODO comments say to.
*/

export default function Item({ item }) {
  return (
    <li>
      {/*
        TODO: render at least two fields from the `item` object.
        Example shape (adjust to your data):
        // <strong>{item.title}</strong> — {item.subtitle}
      */}
    </li>
  );
}
