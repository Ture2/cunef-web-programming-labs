# REFERENCE ONLY — do not copy for your own submission.

This worked example is the static React port of the Block I Riverside FC site. It uses local fixture and squad data, so no backend is needed.

```powershell
npm install; npm run dev
```
