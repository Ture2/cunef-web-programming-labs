{
  "name": "tasks-api-crud-lab",
  "version": "1.0.0",
  "private": true,
  "description": "Session 18 lab — Tasks API full CRUD on an in-memory array (no database). Block II, Week 6, G247 CUNEF.",
  "main": "src/server.js",
  "scripts": {
    "start": "node --watch src/server.js",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
