// REFERENCE ONLY — do not copy for your own submission.
// Squad route that reuses the fetched Riverside FC squad grouping.

import SquadByPosition from "../SquadByPosition";

export default function Squad() {
  return (
    <main>
      <SquadByPosition />
    </main>
  );
}
