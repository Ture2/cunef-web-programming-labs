{
  "name": "riverside-example-auth",
  "version": "1.0.0",
  "private": true,
  "description": "Worked example (read-only) — Riverside FC API with JWT auth, bcrypt login, Zod validation, pagination and admin/member + ownership permissions. Applies the Session 27 pattern to the football domain. Block II, G247 CUNEF.",
  "main": "src/server.js",
  "scripts": {
    "start": "node --watch src/server.js",
    "check": "node check.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
