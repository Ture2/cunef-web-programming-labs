{
  "name": "riverside-example-models",
  "version": "1.0.0",
  "private": true,
  "description": "Worked example (read-only) — Riverside FC relational data layer with node-postgres (parameterized DI models). Applies the Session 22 pattern to the football domain. Block II, G247 CUNEF. Set DATABASE_URL for real Postgres.",
  "main": "src/models/fixturesModel.js",
  "scripts": {
    "db:schema": "psql \"$DATABASE_URL\" -f schema.sql",
    "selftest": "node selfTest.js"
  },
  "dependencies": {
    "pg": "^8.12.0"
  }
}
