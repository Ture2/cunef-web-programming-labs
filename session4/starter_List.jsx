/*
  Starter file for Session 32 Lab — Part 1 (Guided React Practice)
  Web Application Programming (G247) · CUNEF Escuela Politécnica Superior

  Week 11 · Session 32 · Practice (AF2) · Pair work

  Do NOT rename the component (List) and do NOT change the prop name
  (items). The checklist assumes them. You only fill the body where the
  TODO comments say to.
*/

import Item from "./Item.jsx";

export default function List({ items }) {
  return (
    <ul>
      {/*
        TODO: for each element of `items`, render an <Item item={...} />.
        Use the .map() array method — the same one you know from Block I.
        Every mapped <Item /> needs a unique `key` prop; use item.id.

        Skeleton (uncomment and fill in):
        // items.map((item) => (
        //   <Item key={item.id} item={item} />
        // ))
      */}
    </ul>
  );
}
